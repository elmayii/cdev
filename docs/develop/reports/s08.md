# Sprint 08 report — Launch

Five channel drafts in `docs/develop/launch/`, built on the two ratified hooks ("coding
agents work in sessions; CDev turns those sessions into continuous development" and the
spec-driven origin story), each honest about the one-product evidence and the unvalidated
portability claim, each ending with the named human decision:

| Channel | Draft | Pending human act |
|---|---|---|
| Hacker News | `hn.md` (Show HN + first comment) | Submit or not; account; timing |
| Reddit | `reddit.md` | Which subs; account; publish |
| X | `x.md` (6-tweet thread) | Publish; timing |
| LinkedIn | `linkedin.md` | Publish; personal vs page |
| Claude Code communities | `claude-community.md` | Which communities; publish |

Also prepared this sprint: **v0.1.1** (plugin.json bumped, CHANGELOG written — template
encoding fix + branch-prefix unification + community layer). The bump ships with this push;
the GitHub Release act is prepared:
`gh release create v0.1.1 --target main --title "CDev v0.1.1" --notes-file <from CHANGELOG>`
— pending present human authorization, like every publishing act.

## The sprint's own rule, kept

Nothing was posted anywhere. The launch sequence (strategy §22-4: only after the repository
is prepared) also still waits on S06 (README + identity) — publishing before that would
contradict the strategy this plan implements.
