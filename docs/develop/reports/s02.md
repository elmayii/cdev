# Sprint 02 report — Plugin and marketplace

*Contract delta for Sprint 03 (document 08): everything below exists and was verified, not
planned.*

- **Plugin:** `cdev` 0.1.0, manifest at `.claude-plugin/plugin.json` (name, displayName,
  version, description, author, license MIT, repository, keywords). `claude plugin validate` —
  pass.
- **Repositories:** `github.com/elmayii/cdev` (the package, full history) and
  `github.com/elmayii/cdev-marketplace` (marketplace manifest + README). Both public, MIT.
- **Install path:** `/plugin marketplace add elmayii/cdev-marketplace` →
  `/plugin install cdev@cdev-marketplace`. Installs at user scope.
- **Installed layout (verified):** `~/.claude/plugins/cache/cdev-marketplace/cdev/0.1.0/` —
  complete copy of the plugin root: `skills/` (7), `profiles/`, `templates/`, `scripts/`,
  `docs/`. Skills resolve profiles at `../../profiles/` identically in working tree and cache.
- **Namespacing:** the 7 skills load as `cdev:bootstrap`, `cdev:cdev`, `cdev:cdev-planner`,
  `cdev:ockham`, `cdev:bootstrap-monorepo`, `cdev:cdev-monorepo`,
  `cdev:cdev-monorepo-planner` — verified in a plain session with no `--plugin-dir`.
- **Old install:** the nine pre-refactor skills left `~/.claude/skills` (backup in
  `.backups/global-skills-2026-08-15/`); the directory is now empty.
- **Update semantics:** version-gated — installed copies re-fetch only on a `version` bump.
- **Local dev:** `claude --plugin-dir <repo>` loads the working tree live; `/reload-plugins`
  picks up edits; `scripts/sandbox.ps1` still builds exercise fixtures.
