# Sprint 01 report — Consolidation refactor

*The contract delta for Sprint 02 (plugin packaging). What the package now is, what was cut,
what moved where.*

## The shape after the sprint

```
skills/      7 skills: bootstrap · cdev · cdev-planner · ockham
                       bootstrap-monorepo · cdev-monorepo · cdev-monorepo-planner
profiles/    backend.md · frontend.md — each exactly: verified / evidence / hardest gate /
             self-chosen order. Loaded by cdev and bootstrap via real-path ../../profiles/
templates/   render base for bootstrap (+ PLACEHOLDERS.md contract, 34 placeholders).
             Default render = core artifacts only; agents, per-repo skills, night-runner,
             TESTING/RUNBOOK/ROADMAP/ARCHITECTURE render on request
scripts/     sandbox.ps1 · install.ps1 · verify-bootstrap.ps1 (aligned to the core set)
```

This is already the plugin layout: Sprint 02 adds `.claude-plugin/plugin.json` and the
marketplace repo — no further moves expected.

## What was consolidated

- `cdev` + `cdev-backend` + `cdev-frontend` → **one loop**, role deltas read from profiles.
- `bootstrap-backend` + `bootstrap-frontend` + `cdev-bootstrap` → **one conditioner**,
  recognition-first, same profiles materialized into the conditioned repo's protocol.
- The protocol template shrank from a full copy of the generic loop to **deltas-only** —
  the single biggest periphery removal (pnpm hardcodes, model trailer, ghost artifact roster
  all died with it).

## What was cut (doc 07 justification per item)

Reviewer agents + per-repo runtime skills out of the default render (§5, unevidenced) ·
greenfield conditioning-plan template deleted (§4) · product references (Compiss, F6) —
zero remain (§4) · night-runner demoted to optional binding (§4) · one-case CLAUDE template
prose (§4).

## The five field rules, placed

| Rule (doc 07 §6) | Home |
|---|---|
| 1 Wave quiesces before system verification | `cdev-monorepo` §verify |
| 2 Verification debt aggregated, not just recorded | `cdev-monorepo` global report · `cdev-planner` mode 2 |
| 3 Resumption-critical conventions fixed | protocol template (newest-first) + bootstrap declares it + loop reads the declaration |
| 4 Assertions about another repo derived on read | `bootstrap-monorepo` registry + `cdev-monorepo` reconciliation |
| 5 Operational docs checked for contradiction | `bootstrap` recognition step |

## Evidence

Four sandbox exercises by fresh subagents, all outcomes recorded in `AGENT_PROGRESS.md`:

1. **Backend loop** — closed a 2-batch sprint honestly; root-caused a real mid-batch failure.
2. **Frontend loop** — ended `BLOCKED` (runtime evidence impossible), named the minimum human
   decision, flagged the fixture's lying gates. The honesty rule held under pressure.
3. **Conditioner** (pre-B04 templates) — caught the fake test gate, clarity map exactly on
   the DEFINED+PARTIAL boundary, ABSENT got a question, not a batch.
4. **Conditioner re-run** (post-B04 templates) — `BOOTSTRAP VERIFY: PASS`, zero unresolved
   placeholders, conditioning committed. ~30 friction findings across the four runs were
   fixed in place or recorded; the noteworthy environment one: the RTK hook falsifies npm
   exit codes, so gate verification bypasses it via `rtk proxy`.

## Known leftovers (deliberate)

- `{{LATER_SPRINT_TITLES}}` is a single slot for plural sprints — cosmetic, fix on next
  template touch.
- 6 placeholders in the contract serve only optional templates (RUNBOOK/night-runner/ROADMAP).
- Field rule 4 is stated in two homes (creation-time and run-time) — one rule, two moments;
  accepted.
